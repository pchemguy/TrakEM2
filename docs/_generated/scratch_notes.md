# Scratch Notes

- ImageJ plugin page highlights that TrakEM2 handles morphological data mining, 3D modeling, stitching/registration, editing, and annotation; integrates with 3D Viewer; page includes JVM tuning guidance for very large datasets (G1 GC over incremental GC).
- Legacy manual (TrakEM2 0.9a) sections: Introduction, Setup, create/save/open/export projects, modeling basics, usage tutorials, plugin extensibility, using as a framework, appendix. Use as source for historical workflows and UI guidance.
- GitHub wiki currently unreachable via raw URLs; follow-up needed to retrieve page list or confirm availability.
